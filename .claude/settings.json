{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"$f\" | grep -qE '/(web|mobile|packages)/.*\\.(js|jsx|ts|tsx|json|css|scss)$' && pnpm exec biome check --write \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Running biome check..."
          }
        ]
      }
    ]
  }
}
